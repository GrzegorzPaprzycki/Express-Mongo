extends layout

block content
  h1= title
  form(method='get')
    input(type='text' name='search' value=search || '')
    input(type='submit' value='Szukaj')
  each item in data
    h2= item.title
    p= item.created
    p= item.description